var GameFrame = require("<scripts>/components/GameFrame")

var Game = React.createClass({
    render: function() {
        return (
            <GameFrame>
                Hello World!
            </GameFrame>
        )
    }
})

module.exports = Game
